# Build artifacts
/target/
*.wasm
!extension.wasm
*.pdb

# Cargo
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Server binary (downloaded/built separately)
/server/

# Grammars - fetched from remote repository, never commit locally
/grammars/
# But include pre-built wasm for Windows (Zed can't compile grammars on Windows)
!/grammars/stata.wasm
/tree-sitter-stata
scratch.do
callee.do
caller.do

# Downloaded language server for dev testing
/sight-node-*/
/sight-v*/
scratch1.do
