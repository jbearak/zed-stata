# Build artifacts
/target/
*.wasm
!extension.wasm

# Cargo
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Server binary (downloaded/built separately)
/server/

# Grammars - fetched from remote repository, never commit locally
/grammars/
# But include pre-built wasm for Windows (Zed can't compile grammars on Windows)
!/grammars/stata.wasm
/tree-sitter-stata
scratch.do
